*** Keywords ***
Dado que ${keyword}
    Run keyword   ${keyword}

Quando ${keyword}
    Run keyword   ${keyword}

Entao ${keyword}
    Run keyword   ${keyword}

E ${keyword}
    Run keyword   ${keyword}